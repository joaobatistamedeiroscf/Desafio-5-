
// This page is no longer used - redirecting to login
import { Navigate } from "react-router-dom";

const Index = () => {
  return <Navigate to="/login" replace />;
};

export default Index;
